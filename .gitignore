# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (never commit real API keys)
.env
.env.local
.env.*.local
.env.vercel
.env.vercel.tmp
.env.production

# API Keys (never commit)
BETA_KEYS.md
*-keys.md
api-keys.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

find-token.js
temp_token.txt
temp_url.txt

.env*.local

# Phase 7 — repo-local tmp directory for regeneration scripts
# (tests/scripts/regen-sig-cbdb6556.mjs writes tmp/SIG-CBDB6556-regenerated.txt).
# Intentionally NOT /tmp/ so the script is cross-platform.
tmp/
