# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "Ebenova Legal Docs"
slug: "ebenova-legal-docs"

runtime: nodejs

build:
  install: "npm install"

start:
  type: http
  command: "node src/index.js"
  port: 8080

# Environment variables required
env:
  EBENOVA_API_KEY:
    description: "Your Ebenova API key. Get a free key at https://ebenova.dev/dashboard"
    required: true
  EBENOVA_API_BASE:
    description: "API base URL"
    default: "https://api.ebenova.dev"
    required: false
